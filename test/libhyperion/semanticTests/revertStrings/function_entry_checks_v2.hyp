pragma abicoder v2;
contract C {
    function t(uint) public pure {}
}
// ====
// revertStrings: debug
// ----
// t(uint256) -> FAILURE, hex"08c379a0", 0x40, 0x22, "ABI decoding: tuple data too short"
