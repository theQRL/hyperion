contract Context {}

contract SQRCTF1 is Context {
    function approve() public virtual { _approve(); }
    function _approve() internal virtual {}
}

contract __unstable__SQRCTF1Owned is SQRCTF1 {
    function _approve() internal override {
        if (true) {
            super._approve();
        }
    }
}
// ====
// SMTEngine: all
// ----
